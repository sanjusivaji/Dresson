<aside class="w-full md:w-56 shrink-0 space-y-1 font-semibold text-xs uppercase tracking-wider text-gray-500">
    
    <a href="/profile" class="flex items-center px-4 py-3 rounded-sm <%= typeof activeSidebar !== 'undefined' && activeSidebar === 'account' ? 'bg-white text-gray-900 border-l-4 border-gray-900 shadow-sm font-bold' : 'bg-transparent hover:bg-white hover:text-gray-900 transition' %>">
        My Account
    </a>
    
    <a href="/profile/address" class="flex items-center px-4 py-3 rounded-sm <%= typeof activeSidebar !== 'undefined' && activeSidebar === 'address' ? 'bg-white text-gray-900 border-l-4 border-gray-900 shadow-sm font-bold' : 'bg-transparent hover:bg-white hover:text-gray-900 transition' %>">
        My Address
    </a>
    
    <a href="/profile/orders" class="flex items-center px-4 py-3 rounded-sm <%= typeof activeSidebar !== 'undefined' && activeSidebar === 'orders' ? 'bg-white text-gray-900 border-l-4 border-gray-900 shadow-sm font-bold' : 'bg-transparent hover:bg-white hover:text-gray-900 transition' %>">
        My Orders
    </a>
    
    <a href="/profile/wishlist" class="flex items-center px-4 py-3 rounded-sm <%= typeof activeSidebar !== 'undefined' && activeSidebar === 'wishlist' ? 'bg-white text-gray-900 border-l-4 border-gray-900 shadow-sm font-bold' : 'bg-transparent hover:bg-white hover:text-gray-900 transition' %>">
        My Wishlist
    </a>
    
    <a href="/profile/wallet" class="flex items-center px-4 py-3 rounded-sm <%= typeof activeSidebar !== 'undefined' && activeSidebar === 'wallet' ? 'bg-white text-gray-900 border-l-4 border-gray-900 shadow-sm font-bold' : 'bg-transparent hover:bg-white hover:text-gray-900 transition' %>">
        My Wallet
    </a>
    
    <a href="/logout" class="flex items-center px-4 py-3 text-red-600 hover:bg-red-50 transition font-bold rounded-sm normal-case tracking-normal mt-4">
        Logout
    </a>
</aside>